services:
  app:
    depends_on:
      - postgres
    build:
      context: ./
      dockerfile: ./docker/app/Dockerfile
      target: build-release-prod
    networks:
      - mynet
    ports:
      - 3000:3000
    deploy:
      restart_policy:
        condition: on-failure