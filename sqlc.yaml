version: "2"
sql:
  - engine: "postgresql"
    queries: "./db/queries/**/*.sql"
    schema: "./db/migrations"
    gen:
      go:
        package: "queries"
        out: "./internal/adapters/out/postgres/queries"