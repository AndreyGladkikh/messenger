должен ли rest api endpoint возвращать в теле детальную информацию об ошибке, если в процессе обработки запроса произошла ошибка на стороне сервера, то есть http code 500?
что за projections?
что если в базовом ddomainEvent нет поведения? как ловить все доменные события, которые принадлежат одному супер типу, но у супертипа нет поведения
event store это то же самое что и outbox?

должно ли быть событие MessageCreated или достаточно MessageSent?

предположим, на одно событие есть несколько обработчиков. то есть после события, нужно произвести несколько независимых действий. если я опубликую события уже после завершения транзакции, тогда какой смысл в uotbox? какжется, как буд-то он тут неуместен. может просто сделать что-нибудь типа такого: таблицу event_handlings(id, event_code, event_payload, task_code, error) (я думаю, название таблицы не совсем удачное, помоги придумать лучше), и в нее записывать только неудашиеся попытки конкретных обработчиков. event_code - название события, event_payload - его тело, task_code - конкретный обработчик события, одна из реакция, которая должна случиться в ответ на событие. например, после отправки события нужно отправить уведомления, пересоздать query model, и, например, отправить куда-нибудь метрики для аналитики. допустим, отправлено сообщение, уведомления успешно отправились, query model успешно пересоздалась, а в процессе отправки метрик произошла ошибка, тогда записываем в event_handlings только то, что не удалось отправить метрики по такому-то событию и ошибку. может есть какой-то паттерн для решения такой задачи?